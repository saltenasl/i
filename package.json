{
  "name": "agents-first-electron-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "mkdir -p .local/data/users && PRIMARY_DB_PATH=$(pwd)/.local/data/primary.sqlite USER_DATA_DIR=$(pwd)/.local/data/users concurrently -k -n renderer,server \"pnpm --filter @repo/renderer dev\" \"pnpm --filter @repo/server dev\"",
    "build": "pnpm --filter @repo/renderer build && pnpm --filter @repo/server build",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "type-safety:check": "node --import tsx scripts/check-type-safety.ts",
    "arch:check": "node --import tsx scripts/check-architecture.ts && node --import tsx scripts/check-no-mocks.ts && node --import tsx scripts/check-auto-extract-test-policy.ts",
    "db:generate-types": "node --import tsx packages/db/scripts/db-generate.ts",
    "db:verify": "node --import tsx packages/db/scripts/db-verify.ts && node --import tsx scripts/check-migration-discipline.ts",
    "test:backend": "vitest run -c packages/auto-extract/vitest.config.ts && vitest run -c packages/db/vitest.config.ts",
    "test:server": "vitest run -c apps/server/vitest.config.ts",
    "test:rtl": "vitest run -c apps/renderer/vitest.config.ts",
    "test:e2e": "pnpm build && playwright test -c apps/server/e2e/playwright.config.ts",
    "verify": "env -u NO_COLOR pnpm lint && env -u NO_COLOR pnpm typecheck && env -u NO_COLOR pnpm type-safety:check && env -u NO_COLOR pnpm arch:check && env -u NO_COLOR NODE_OPTIONS=--disable-warning=ExperimentalWarning pnpm db:verify && env -u NO_COLOR NODE_OPTIONS=--disable-warning=ExperimentalWarning pnpm test:backend && env -u NO_COLOR NODE_OPTIONS=--disable-warning=ExperimentalWarning pnpm test:server && env -u NO_COLOR NODE_OPTIONS=--disable-warning=ExperimentalWarning pnpm test:rtl && env -u NO_COLOR NODE_OPTIONS=--disable-warning=ExperimentalWarning pnpm test:e2e",
    "prepare": "simple-git-hooks"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.44",
    "@ai-sdk/google": "^3.0.33",
    "@ai-sdk/openai": "^3.0.29",
    "@hono/node-server": "^1.19.2",
    "@repo/auto-extract": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/server": "workspace:*",
    "@repo/test-utils": "workspace:*",
    "ai": "^6.0.86",
    "dotenv": "^17.2.3",
    "hono": "^4.7.2",
    "jose": "^6.0.8",
    "kysely": "^0.28.2",
    "nanoid": "^5.1.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@playwright/test": "^1.52.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^22.15.30",
    "@types/react": "^19.1.6",
    "@types/react-dom": "^19.1.5",
    "@vitejs/plugin-react": "^4.4.1",
    "concurrently": "^9.1.2",
    "jsdom": "^26.1.0",
    "simple-git-hooks": "^2.13.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm verify"
  }
}
